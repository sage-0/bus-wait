# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト状態

**実装未着手。** 現時点でリポジトリにあるのは設計書 (`docs/design.md`) のみ。

**端末側スタックは確定済み (2026-08-10)。サーバー側は未決定。**

- 端末側: **Flutter 1本 + 各OSネイティブの薄いバックグラウンド層 (Swift / Kotlin)**。設計書 4.5 が正。`background_geolocation` (transistorsoft) は初期採用せず、OEM キルが顕在化した場合の差し替え先として保留 (4.3)
- サーバー側: 6.1 が `jsonb` を使うため PostgreSQL 系を想定。言語・フレームワークは未定
- 5.2 の1次元クラスタリングと 5.6 のパーセンタイル算出が集計処理の中核であり、スタック選定はこれを書きやすいかで評価すべき

**想定規模は利用者千人超。** ピーク時の同一のりば待機人数は時間帯による差が極端 (1・2限後は数人、大コマ終了後は300人超)。この規模を前提に、送信ジッター (4.6)、`motion_sample` のサンプリング (6.2)、`raw_events` の月次パーティショニング (6.1) が設計に組み込まれている。

## コマンド

```bash
flutter analyze app                                    # Dart 静的解析
cd app && flutter build ios --debug --no-codesign      # iOS ビルド検証
cd app && flutter run -d <device-id>                   # 実機実行 (flutter devices で ID 確認)
python3 scripts/analyze_probe.py <a.jsonl> [<b.jsonl>] # 実測データの解析
```

Flutter は Homebrew cask で導入済み (`/opt/homebrew/bin/flutter`)。iOS deployment target は 15.0
(`CLLocationManager.authorizationStatus` 等が iOS 14+ のため 13.0 から引き上げた)。

実測プロトタイプの使い方は `app/README.md`。サーバー側はまだ存在しない。

## 設計書

`docs/design.md` が唯一の仕様源。以下は「読まずに実装すると確実に踏み外す判断」の要約であり、詳細・根拠は設計書の該当章を参照すること。

## 死守すべき設計上の不変条件

これらは制約の下で意図的に選ばれた判断であり、一見冗長・遠回りに見える。「単純化」しないこと。

### 1. 人数を数えない (1.3)

混雑度は待ち行列の人数ではなく、**利用者一人あたりの「バス停到着 → 乗車」所要時間の分布**から導く。アプリ普及率が不明なため、観測人数から実人数を推定する経路は原理的に成立しない。人数カウントに見える機能を追加してはならない。

### 2. 端末は判定しない (2.1)

端末の責務は**生イベントの記録・キューイング・送信のみ**。滞在時間の算出、のりばの最終確定を除く判定、混雑度の計算はすべてサーバー側。

- `POST /v1/events` のレスポンスに判定結果を含めない
- iOS のリージョン退出は独自ヒステリシスで数分遅れるため、**端末が算出した滞在時間・退出時刻を信用する実装を書かない**
- Android の `GEOFENCE_TRANSITION_DWELL` は利用可能だが**使わない**。iOS に存在せず、使うと OS 差がそのまま仕様差になる

### 3. `occurred_at` と `received_at` を混同しない (4.1)

オフライン時は端末にキューイングして復帰時に一括送信するため、送信遅延は常態。`occurred_at` は必ず**イベント発生時刻**を保持する。`event_id` (端末生成 UUID v4) によりサーバー側は冪等に処理する。

### 4. キャンパス側は2段構え。半径100m未満のジオフェンスを作らない (3.2)

キャンパス側の2のりばは約113mしか離れていない。iOS のリージョン監視は基地局・Wi-Fi測位ベースで、**半径100m未満では発火が不安定**になる。よって:

- 第1段: 中間点に `campus_area` (半径150m) を**1つだけ**登録 — 起床トリガー専用、のりば判別はしない
- 第2段: 起床後に前景で高精度測位 (最大60秒)、2点との距離差が35m以上なら確定、未満なら `pending`

「のりばごとにジオフェンスを張れば単純になる」は既に検討され、上記の理由で棄却済み。

### 5. 監視リージョンは3件のみ (3.1)

`campus_area` / `st_hachioji` / `st_minamino`。`campus_hachioji_bound` と `campus_minamino_bound` は**測距用の座標であってジオフェンスではない**。この区別を崩さないこと。座標値は設計書 3.1 / 3.2 が正。

八王子駅とみなみの駅は約2.7km離れており、**どちらのリージョンに入ったかで系統が一意に決まる**。系統を利用者に選ばせる UI は不要 (検討済み・破棄) (3.4)。

### 6. 発車イベントは着地点で検出する。二層構造 (5.2)

**出発側の `region_exit` だけを見る方式は棄却済み** (v0.1 の設計)。退出検知の遅延が発車間隔と同オーダーに達すると DBSCAN が複数便を1クラスタに融合し (chaining)、**測りたい混雑時ほど壊れる**。閑散時は正常に動くため小規模テストでは発覚しない。パラメータ調整では解決しない。

| | 層1 (確定) | 層2 (速報) |
|---|---|---|
| 観測点 | **着地点の `region_enter`** | 出発側の `region_exit` + 時刻表 |
| 用途 | `missed_buses`、履歴、ベースライン | リアルタイム表示のみ |
| 遅延 | 15〜35分 | 即時 |

同一便の乗客は1台の車両に同乗しているため、**着地点リージョンに物理的に同時刻に進入する**。これが層1の根拠。

- **`missed_buses` と履歴には層1 (`layer = confirmed`) のみを使う。** 層2の値を履歴・ベースラインに混入させない
- **層1では中央値を使わない。** 便が識別済みなら遅延 ε は片側 (ε ≥ 0) にしか分布せず、中央値は系統的に遅れる。下位10〜20パーセンタイルを用いる。**層2は中央値のまま** (便が未識別のため)
- 利用者が1人の時間帯は時刻表上の最も近い発車時刻を使い `confidence` を下げる — エラーにしない
- **層1の前提 (着地点のばらつきが出発側より桁で小さい) は未検証**。11章の実測が先 (未決事項#7)

### 7. 乗車確定は往復照合。速度判定はフォールバック (5.4)

**着地点リージョンへの進入をもって乗車を確定する**。バス停エリアに入っただけの通過者 (コンビニ客、講義棟の在室者、駅前通行人) を排除する主要な仕組みはこれであり、滞在時間の下限フィルタではない (3.3)。照合は両方向で対称に成立する。

「道路方向に20km/h以上で離脱」という速度ベースの判定は、**照合成立前の暫定シグナルおよびフォールバック**としてのみ残す。乗車確定の主経路にしない。

照合ウィンドウ 15〜35分は**暫定値**。`trip_matches.elapsed_seconds` の実測分布で較正する前提 (未決事項#2)。ハードコードせず設定値として持つこと。

### 8. 除外は統計からのみ。生データは消さない (5.5)

`wait_seconds < 60秒` / `> 60分`、`validated_by = none`、`stop_id = pending` のセッションは統計から除外するが、**`raw_events` は保持する**。しきい値 (35m, 15〜35分等) の事後較正がこのデータに依存している。

### 9. 混雑度は絶対値ではなく相対パーセンタイル (5.6)

`同一のりば × 同一曜日 × 同一時限帯` の履歴分布に対する、直近ウィンドウの待ち時間中央値のパーセンタイル。「待ち10分」ではなく「同条件の履歴で上位10%」に意味がある。`missed_buses` の直近平均も主要指標 — 体感の混雑は列の長さではなく「1本目に乗れるか」で決まる (5.3)。

サンプル数が閾値未満なら**混雑度を算出せず、その旨を明示する**。欠測を平均値等で埋めない。

指標は2系統ある (5.6)。

| | 5.6.1 確定指標 | 5.6.2 即時指標 (層0) |
|---|---|---|
| 由来 | 層1 (着地点クラスタ) | `region_enter` = 未クローズのセッション数 |
| 遅延 | 15〜35分 | ほぼゼロ |
| 用途 | **履歴・baseline の構築**、`missed_buses` | **現在値の表示** (3分更新) |

- **baseline の構築に層0を使わない。** 往復照合を経ておらず通過者を含むため、履歴が汚染される
- **層0の baseline は直近数週間に限定する。** 普及率が学期をまたいで変動すると相殺が壊れるため (未決#14)
- 層0が内部でセッション数を用いることは、それを**表示・API 出力してよいという意味ではない** (7.6)

### 10. ジオフェンス発火時に Dart を起こさない (4.5)

イベントの記録・キュー・送信は**ネイティブ層 (Swift / Kotlin) で完結**させる。Dart の責務は UI・権限フロー・設定更新・状態表示のみ。

- Flutter の background isolate (`@pragma('vm:entry-point')`) をイベント経路に使わない
- **設定はネイティブ側に永続化する**。アプリ終了後の発火時に Dart は存在しないため、ネイティブ層は自身の永続設定のみで動作できなければならない。Dart は設定を「更新する」のであって「供給する」のではない
- Dart がイベントを生む唯一の経路は `enqueueBoardingHint` (4.7)

### 11. しきい値をアプリに埋め込まない (6.2)

リージョン定義と較正値は `GET /v1/config` で配信し、ネイティブ側にキャッシュする。埋め込むと較正のたびに再ビルドと再配布 (TestFlight の審査待ちを含む) が必要になる。

### 12. キュー溢れ時に `region_enter` / `region_exit` を捨てない (4.6)

破棄してよいのは `motion_sample` のみ (5.4 のフォールバックと較正専用のため、欠落しても待ち時間統計は成立する)。`region_enter` / `region_exit` / `stop_resolved` / `boarding_hint` はセッションそのものを構成するので破棄しない。

`motion_sample` は `GET /v1/config` の `motion_sample.rate` により**一部の端末からのみ**収集する。千人規模では全数収集すると年間1億行のオーダーに達し、かつ較正目的には全数が不要なため。

### 13. 端末時計のずれは検出するが端末側で補正しない (4.6)

5.2 のクラスタリングは複数端末の `occurred_at` を横断比較するため、時計ずれがクラスタを壊す。リクエストの `device_sent_at` と `received_at` の差からサーバー側が検出し、閾値超過分をクラスタリングから除外する。**端末側での補正は判定であり、不変条件2に反する。**

### 14. 認証サーバーと収集サーバーを分離する (9.2)

- 認証側 (`auth_registrations`) に **`anon_token` を保存しない**。`sub` 自体も保存せずハッシュのみ
- `anon_token` はランダム生成。**決定的ハッシュ (`HMAC(secret, sub)`) を使わない** — 学籍番号ベースのメールは候補が有限で、`secret` 漏洩時に総当たりで全員逆引きできるため
- 両サーバーのログを突合しない、認証ログは7日保持 — これは**技術的保証ではなく運用上の約束** (9.3)。利用者への説明でも「技術的に不可能」と書かない
- 収集サーバーとは**別のデータベース**に置く

### 15. 表示は4段階のみ。人数を出さない (7章)

| 段階 | パーセンタイル | 意味 |
|---|---|---|
| Lv.0 | 〜50 | 待たずに乗れる見込み |
| Lv.1 | 50〜75 | 通常 |
| Lv.2 | 75〜90 | やや待つ |
| Lv.3 | 90〜 | **1本見送る可能性がある** |

- 境界は等間隔にしない。混雑側ほど識別の価値が高いため上位を細かく取る
- **Lv.3 は `missed_buses` の言語化**。5.3 の主要指標と表示を一致させている
- **主表示は「現在値」「この時間帯の通常値」「次の発車まで」の3要素** (7.1)。現在値のみだと大コマ終了直前に最も誤った情報を出す
- **データ不足を Lv.0 として表示しない** (7.3)。「空いている」ではなく「分からない」。代わりに時刻表から「次の発車まで○分」を出す
- 待機人数・セッション数・待ち時間の絶対値を主表示にしない (7.6)
- 4段階を超えて細分化しない。統計的精度が伴わず、7.5 のフィードバックループを強める

### 16. 測位ウィンドウを広げない (9.1)

「大学と最寄り2駅の周辺以外では位置を一切記録しない」という利用者への保証は、4.2 の測位ウィンドウ限定 (判別中60秒 / `region_exit` 前後120秒) にのみ依存している。

**ウィンドウの拡大はプライバシー方針の変更であり、技術的判断だけで行ってはならない。**

### 17. データは削除ではなく段階的匿名化 (9.4)

`motion_sample` のみ90日で削除。他は13ヶ月後に `anon_token` を除去して保持する。5.5 の「生データは消さない」と両立させるための方式。

5.6 のサンプル数閾値は統計的信頼性と k-匿名性の**二重の役割**を持つ。閾値は両者の要求の**大きい方**を採る (9.5)。

## 実装着手前に解決が必要な事項

7章・9章は v0.2 で確定済み。**8章 (コールドスタート期間の運用) のみ未着手。**

- **未決事項#12 — Google Workspace の OAuth 承認** (最優先ブロッカー)。`edu.teu.ac.jp` の管理者が外部アプリを制限していると**認証そのものが成立しない**。他のどの作業より先に確認する
- **11章の実測プロトタイプが最初の実装タスク**。特に「**同一のバスに乗せた2端末の `region_enter` 発火時刻差**」は 5.2 層1の成否を単独で決める。数十秒に収まれば層1は成立し、数分に及べば 5.2 の再設計をやり直す必要がある。**この測定より先に集計側を作り込まないこと**
- **未決事項#13 — 学内サーバーの利用申請**。リードタイムが読めないため早期着手。「個人開発だから学内規程の適用外」は成立しない (9.7)
- **8章 コールドスタート期間の運用** — 未検討。ただし時刻表 (5.2.5) と 7.3 のフォールバックにより、履歴が皆無でも画面が成立する構造は既にある
- 較正が前提の値: 判別しきい値35m (3.2)、照合ウィンドウ15〜35分 (5.4)、到着クラスタの eps 45秒・下位パーセンタイル (5.2)

これらに触れる実装をする場合は、勝手に決めずユーザーに確認すること。

## 権限フロー (4.4)

初回起動でいきなり「常に許可」を要求しない。「使用中のみ許可」で体験させ、利用実績が生じた後に昇格を促す。当面は TestFlight / 内部配布のためストア審査対応は範囲外 (1.2)。
