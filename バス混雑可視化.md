# スクールバス混雑可視化システム

**最終更新**: 2026-08-10

東京工科大学 八王子キャンパス スクールバス (八王子駅 / 八王子みなみ野駅 系統) の
待ち行列の混雑状況を、**追加の固定設備なしに**可視化する。

> このファイルは MTG 用の資料であり、毎回更新する。
> 技術的な詳細と根拠は設計書 `docs/design.md` (v0.2) が正。

---

## 1. 何を作るか

利用者が「今バス停に向かうと、どのくらい待つか」を事前に把握できるようにする。

### 中核となる考え方: 人数を数えず、待ち時間を測る

待ち行列の人数を数える方式は採らない。**アプリの普及率が不明である以上、観測した人数から
実際の人数を推定する経路は原理的に成立しない**ため。

代わりに、利用者一人あたりの「バス停到着 → 乗車」の所要時間を実測し、その分布から混雑度を導く。
この方式は観測者が1人でも成立し、普及率に依存しない。

### やらないこと

| 項目 | 理由 |
|---|---|
| 正確な待ち人数の計測 | 上記の通り原理的に成立しない |
| 固定ハードウェアの新規設置 | 制約条件 (BLE ビーコンは将来の任意拡張として保留) |
| 未登録者の計測 | アプリ登録者のみを対象とする |
| 秒単位のリアルタイム性 | OS のバックグラウンド制約と両立しない |

---

## 2. 現在の状況 (2026-08-10)

### 完了

| | |
|---|---|
| 設計書 | v0.2。**未着手は8章 (コールドスタート運用) のみ** |
| 端末側スタック | Flutter + 各OSネイティブの薄い層 (Swift / Kotlin) に確定 |
| Google 認証の可否 | **確認済み**。大学アカウントで「内部アプリ」として作成でき、審査も管理者承認も不要 |
| 実測プロトタイプ | iOS 版の実装完了・ビルド成功。解析スクリプトも用意済み |

### 進行中

- **実測プロトタイプによる検証** — iPhone 2台を同じバスに乗せ、検知遅延を測る

### 未着手

- Android 版 (iOS の測定が回り始めてから)
- サーバー側の実装
- 設計書8章 (コールドスタート期間の運用)

---

## 3. 設計の要点

詳細は設計書を参照。判断の根拠のみ記す。

### 端末は判定しない

端末の責務は生イベントの記録・送信のみとし、滞在時間の算出も混雑度の計算もすべてサーバー側で行う。

iOS のリージョン退出判定には独自の遅延があり、端末が算出した滞在時間は信用できない。また
Android には iOS に無い機能があり、判定を端末に置くと OS 差がそのまま仕様差になる。

### 発車時刻は「着地点」で検出する

当初は「バス停から複数人が同時に離れた時点」を発車とみなす設計だった。**これは混雑時に破綻する**
ことが判明したため再設計した。

> 退出検知には分単位の遅れがあり、増発でバスの間隔が詰まると、前後の便の乗客が混ざって
> 区別できなくなる。**測りたい混雑時ほど壊れる**という致命的な性質を持つ。

代わりに**到着側**で検出する。同じバスに乗った人は、目的地のバス停に**物理的に同時刻に到着する**
ため、こちらは分離しやすい。乗客の端末を通じて、実質的にバス車両の位置を観測していることになる。

- **確定値** (到着側・15〜35分遅れ) — 履歴と「1本見送ったか」の算出に使う
- **速報値** (出発側・即時) — リアルタイム表示に使う

### 混雑度は絶対値ではなく相対値・4段階

「今4人待っている」には意味がないが、「同じ曜日・同じ時限の履歴で上位10%」には意味がある。

| | 意味 |
|---|---|
| Lv.0 | 待たずに乗れる見込み |
| Lv.1 | 通常 |
| Lv.2 | やや待つ |
| Lv.3 | **1本見送る可能性がある** |

体感の混雑は列の長さではなく「1本目に乗れるか」で決まるため、最上位の段階をそれに直結させている。

**現在値と「この時間帯の通常値」を併記する。** 現在値だけを出すと、大コマ終了直前 —— 最も情報が
必要な瞬間 —— に「空いています」と表示してしまうため (授業がまだ終わっていないので実際に空いている)。

### 認証と収集を分離する

大学の Google アカウントで利用者を限定するが、**アカウントと位置情報を紐付けない**。

素直に実装すると「誰が、何限に大学へ来て、何時に帰ったか」の数ヶ月分の記録が個人に紐付く。
バス停の記録だけでも生活パターンはほぼ完全に復元できてしまう。

- 認証サーバーは「登録済みか」だけを知り、発行した匿名IDを保存しない
- 収集サーバーは匿名IDのみを知り、それが誰かを知らない
- 記録するのは**大学と最寄り2駅の周辺のみ**。それ以外の場所では一切記録しない

---

## 4. 懸念点・相談したいこと

### A. 【要相談】学内サーバーの利用申請

サーバーは学内に置く方針だが、情報システム部門への申請が前提になる。

**申請した時点で大学は本システムを認知した状態になり、「個人開発だから学内規程の適用外」という
整理は成立しなくなる。** 大学側にも一定の責任が生じるため、相応の審査が入ると想定している。

- 窓口と手続きを知りたい
- リードタイムが読めないため、早期に着手したい

### B. 【要相談】位置情報の収集と学内規程

学生の位置情報を継続的に収集するため、規程との整合を確認したい。

大学の後ろ盾がない有志プロジェクトが預かる以上、**規程が要求する水準より厳しい水準を自主的に
設定する**方針を採っている。

| 項目 | 方針 |
|---|---|
| 記録範囲 | 大学と最寄り2駅の周辺のみ。それ以外では一切記録しない |
| 匿名ID | 認証と収集を分離。Google アカウントとの対応関係を保存しない |
| 座標データ | 90日で削除 |
| その他 | 13ヶ月後に匿名IDを除去 (個人の行動履歴ではなくなる) |
| 説明 | 大学公式サービスではないことを明示した上で同意を得る |

なお、技術的な分離だけでは完全な匿名化にならない (両サーバーのログを突合すれば紐付けは可能)。
この点は「そうしない」という運用上の約束として明示する方針であり、**技術的に不可能であるかのように
説明しない**。

### C. 【要確認】バス運行の実態

設計に直結するため確認したい。時刻表 PDF は取得済み。

1. **増発は事前にダイヤで決まっているか、現場判断か**
   - ダイヤ記載であれば、時刻表を強い事前情報として使える
   - 現場判断だと、混雑時ほどダイヤから外れるため事前情報としての価値が下がる
2. **駅に到着したバスは、そのまま折り返して発車するか**
   - 折り返し運用であれば、到着の検知が「次の発車」の予告として使える
   - 別車両が来る運用だと使えない

### D. 【要相談】先行事例との連携

戀津魁助教 (メディア学部) が同一路線で iPhone 専用・前景のみの GPS 所要時間計測アプリを
構築済みと聞いている。伏見先生経由での接続を検討したい。

本システムは **Android 対応・完全バックグラウンド・リアルタイム集計**の3点で差別化される。

### E. 技術的な最大リスク (相談不要・進行中)

「発車を混雑時に識別できるか」がシステム全体の成否を決める。再設計した方式は
**同じバスに乗った人は目的地に同時に着く**という物理的な前提に立っているが、
スマートフォンがそれを十分な精度で検知できるかは未検証。

iPhone 2台を同じバスに乗せて実測する。発火時刻の差が数十秒に収まれば成立、数分に及べば再設計。

### F. 費用

| 項目 | 費用 | 時期 |
|---|---|---|
| Apple Developer Program | $99 / 年 | **配布段階で必要**。開発・検証段階は無料で可能 |
| Google Play Developer | 不要 | 内部配布のため |
| サーバー | 学内利用なら不要 | |

---

## 5. 次のアクション

| | 内容 | 担当 |
|---|---|---|
| 1 | 実測プロトタイプで検知遅延を測る (2端末) | 自分 |
| 2 | 学内サーバーの利用申請 | 要相談 (A) |
| 3 | バス運行の実態確認 | 要確認 (C) |
| 4 | Android 版の実装 | 自分 |
| 5 | 設計書8章 (コールドスタート運用) | 自分 |

---

## 更新履歴

### 2026-08-10

- 端末側スタックを Flutter + ネイティブ薄層に確定
- 発車イベント検出方式を再設計 (出発側 → 着地点)
- 混雑度の提示方法 (7章)、プライバシー方針 (9章) を策定
- Google 認証の可否を確認 — **問題なし**
- 実測プロトタイプ (iOS) を実装、ビルド成功
